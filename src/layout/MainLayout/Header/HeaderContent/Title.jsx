// material-ui
import { Box, Typography } from '@mui/material';

// ==============================|| HEADER CONTENT - TITLE ||============================== //

const Title = () => (
    <Box sx={{ width: '100%', ml: { xs: 0, md: 1 } }}>
        <Typography variant="h4">Nusa Customer Dashboard</Typography>
    </Box>
);

export default Title;
